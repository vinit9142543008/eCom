import { BtnDngDirective } from './btn-dng.directive';

describe('BtnDngDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnDngDirective();
    expect(directive).toBeTruthy();
  });
});
