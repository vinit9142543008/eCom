import { BtnPrDirective } from './btn-pr.directive';

describe('BtnPrDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnPrDirective();
    expect(directive).toBeTruthy();
  });
});
