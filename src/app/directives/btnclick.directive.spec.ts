import { BtnclickDirective } from './btnclick.directive';

describe('BtnclickDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnclickDirective();
    expect(directive).toBeTruthy();
  });
});
